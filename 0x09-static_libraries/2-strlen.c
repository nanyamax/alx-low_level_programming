#include "main.h"
#include <string.h>
/**
 *  _strlen - returns the length of a string
 *
 *  @s: pointer
 * Return: Always 0
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);

	return (length);
}
