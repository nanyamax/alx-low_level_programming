#include <stdio.h>
/**
 * main - Entry point
 * Takes no argument
 *
 * Description: print a string to standard output using printf
 * Return: Always 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

