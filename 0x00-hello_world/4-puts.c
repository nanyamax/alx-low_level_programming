#include <stdio.h>

/**
 * main - Entry point
 * Takes no argument
 *
 * Description: printing a script to standard output
 * Return: always 0 (success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

